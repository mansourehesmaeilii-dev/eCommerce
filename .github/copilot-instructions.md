# Copilot Instructions

## Project Guidelines
- User prefers replacing feature-specific result wrappers with a reusable generic base result class that can be used across the project.
- User prefers applying DDD pattern across modules (including Auth), with thin controllers and separated application/domain/infrastructure logic.